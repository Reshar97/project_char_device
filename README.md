Драйвер символьного устройства my_buffer

Это учебный проект: драйвер символьного устройства для ядра Linux, реализующий кольцевой (циклический) буфер.

1. Описание проекта

Драйвер создает виртуальное устройство /dev/my_buffer, которое работает как буфер обмена данными фиксированного размера (1024 байта). 
Он поддерживает операции чтения, записи и управления через ioctl. 
Также реализован интерфейс мониторинга состояния в /proc/my_buffer.

2. Сборка и установка

Для сборки и управления модулем используется система make.

Предварительные требования:
Установите заголовочные файлы ядра:

sudo apt-get install build-essential linux-headers-$(uname -r)

Команды:

    Сборка модуля:

    make

    Загрузка модуля в ядро:

    sudo make install

    Выгрузка модуля из ядра:

    sudo make uninstall

    Очистка проекта:

    make clean

3. Использование устройства /dev/my_buffer

После загрузки модуля в системе появляется устройство /dev/my_buffer.

    Запись данных:

    echo "Привет, ядро!" > /dev/my_buffer

    Чтение данных:

    cat /dev/my_buffer

    Проверка логов ядра (для отладки):

    dmesg | tail -n 20

4. Интерфейс /proc/my_buffer

Драйвер предоставляет интерфейс для мониторинга состояния буфера.

    Команда:

    cat /proc/my_buffer

    Пример вывода:

    --- Состояние кольцевого буфера ---
    Занято: 15 байт
    Свободно: 1009 байт
    Read Ptr: 0
    Write Ptr: 15

5. Управление через IOCTL

Для управления параметрами драйвера (получение размера, очистка буфера) необходимо использовать пользовательское приложение на языке C.
В папке проекта лежит файл test_buffer.c — это готовая интерактивная консоль для тестирования всех функций драйвера.

Компиляция тестового приложения:

gcc test_buffer.c -o test_buffer

Запуск:

./test_buffer

В консоли доступны команды: write, read, size, used, free, clear.
Так же на экран будет выведена подсказка по основным командам в консоли:

=== Интерактивная консоль RingBuffer ===

Доступные команды:
  write <текст> - Записать строку в буфер
  read          - Прочитать всё содержимое буфера
  size          - Общий размер буфера
  used          - Сколько байт занято
  free          - Сколько байт свободно
  clear         - Очистить буфер
  exit          - Выход

ring_buffer> exit

6. Структура проекта

    char_dev.c — исходный код модуля ядра.
    my_buffer_ioctl.h — заголовочный файл с кодами команд ioctl.
    test_buffer.c — тестовое приложение для пользователя.
    Makefile — скрипт для сборки модуля и приложения.
    README.md — текущая документация.
